demo/data
demo/media
Credentials.plist
.vscode
venv

*.app.dSYM.zip
*.ipa

.env.default
*.p8

*.xcloc
# The Xcode project is generated by XcodeGen from project.yml; do not commit it.
/swift-paperless.xcodeproj
# Per-user SwiftPM/Xcode state inside local packages.
**/.swiftpm/
*.afdesign~lock~
.env
scripts/frames
.cache/swpngx

/vendor
/build

# Xcode auto-extracts untabled string literals into the default "Localizable"
# table on IDE builds (an SPM-package quirk we can't disable). The real catalog is
# App.xcstrings; this orphan only ever holds junk, so ignore it everywhere.
Localizable.xcstrings
